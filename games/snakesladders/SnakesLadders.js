
export const settings = {
  rowCount: 5,
  colCount: 8,
};

export const length = settings.rowCount * settings.colCount

export const rollDice = (number) => {
  let random = Math.floor(Math.random() * number)
  return random
};

export const snakeDown = (idx) => {
  let newIdx

  return newIdx
};

export const ladderUp = (idx) => {
  let newIdx

  return newIdx
}

